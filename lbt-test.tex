\documentclass{article}
\usepackage{lbt}
\begin{document}
% This document doesn't produce anything; it doesn't even finish compilation.
% It exists only to call the \lbtRunUnitTests command, which calls
% lbt.test.run_tests(), which then exits.
\lbtRunUnitTests
\end{document}
